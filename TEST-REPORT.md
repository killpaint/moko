# 验证报告

## 实际测试环境

Linux 容器内 Chromium 143 无头浏览器，1600 × 1000 桌面视口；额外检查 390 × 844 窄屏。测试工具为 Playwright。

环境管理策略禁止页面导航，因此通过 set_content 将本地 HTML/CSS/JavaScript 原样注入隔离页面。该页面是 opaque origin，浏览器不提供 WebGPU 安全上下文，也拒绝 IndexedDB/localStorage。所有下列实际画布交互检查在 **Canvas 2D 兼容后端** 下完成。

没有修改浏览器管理策略，也没有将软件/兼容渲染的结果冒充硬件 GPU 跑分。

## 结果：28 / 28 通过

| 检查 | 结果 |
| --- | --- |
| 中文工作台加载 | 通过 |
| 示例含 161 个可编辑图层、两页 | 通过 |
| 指针绘制矩形 | 通过 |
| 指针拖拽改变坐标 | 通过 |
| 撤销拖拽 | 通过 |
| 重做拖拽 | 通过 |
| 八向控制柄缩放 | 通过 |
| 属性面板圆角编辑 | 通过 |
| 填充颜色更新 | 通过 |
| 快捷键复制图层 | 通过 |
| 编组与层级树 | 通过 |
| 解组并保留子层 | 通过 |
| 自动布局间距与位置 | 通过 |
| 创建关联组件实例 | 通过 |
| 主组件更新同步实例 | 通过 |
| 中文多行文字编辑 | 通过 |
| 字号行高字距与对齐 | 通过 |
| 钢笔贝塞尔路径绘制 | 通过 |
| 生产路径 PNG 2× 导出 | 通过 |
| SVG 保留文字与矢量路径 | 通过 |
| SVG XML 格式有效 | 通过 |
| 项目序列化验证与回读 | 通过 |
| 拒绝恶意图层 ID 注入 | 通过 |
| 画板演示预览 | 通过 |
| 真实 2,000 图形测试页 | 通过 |
| 撤销整页测试数据 | 通过 |
| 390px 窄屏基础画布 | 通过 |
| 脚本无运行时异常 | 通过 |

PNG 导出通过生产导出路径生成了真实的 750 × 1560 文件；SVG 通过 XML 解析，包含文字与矢量 path。项目数据通过 JSON 往返和导入验证器检查。

## 本轮已修复

新建文本时浏览器的默认指针焦点转移曾使编辑框立即失焦；已阻止画布默认焦点行为，由编辑器主动管理焦点，并重新通过中文多行文字测试。多字符中文属性标签的换行问题也已修复。

## 未完成的环境验证

- WebGPU pipeline 的实际 GPU 编译/执行与渲染结果。
- 真正硬件 GPU 的帧时间、吞吐量和极限场景。
- 正常网页来源下 IndexedDB 的跨刷新、跨标签页及重启恢复。
- Safari、Firefox、Windows、macOS 和移动设备的完整兼容性。
- 剪贴板/下载权限在各浏览器环境下的完整端到端覆盖。

单文件版还通过了内联构建检查；本报告不是生产上线验收、安全审计或与 Figma 完全等价的证明。

## 运行截图

`preview.png` 是本轮真实运行截图。左下角显示 Canvas 2D，顶部显示临时编辑，这是测试环境能力的真实反馈，不是图像后期修改。
