# 墨构实现说明

## 1. 零运行时依赖

`index.html` 依次载入 `core.js`、`renderer.js`、`app.js`，通过 `window.Moko` 共享小型命名空间。没有动态 import、远程字体或 CDN，因此可被 `build.mjs` 直接内联成一个 HTML。

`server.mjs` 仅负责静态文件，没有应用后端。代码使用 Node 内置 http/fs/path，监听 127.0.0.1，拒绝路径逃逸；不会安装依赖或打开远程端口。

## 2. 文档与变换

文档结构为 version → pages → children，每个图层有稳定 ID、类型、名称、局部坐标、尺寸、角度、外观及可选子层。所有文档均可用 JSON 序列化。

`flatten()` 计算父子仿射变换、世界坐标边界、逆矩阵、继承的不透明度/锁定/可见状态以及祖先裁剪链。命中检测先转换到图层局部空间；矢量路径由 Canvas 的 path/stroke 命中方法检测。

编组和改父级利用世界矩阵与父级逆矩阵保留视觉位置。自动布局的尺寸与间距直接写回文档子层，因此画布、导出与属性面板使用同一份数据。

## 3. GPU 绘制

WebGPU 后端包含两个 pipeline：

1. 全屏背景与可切换的点阵网格。
2. 实例化形状/纹理四边形。

每个实例占 128 字节：变换基、平移与尺寸、两端填充色、描边色、形状参数、图集 UV、不透明度、裁剪起点和数量。祖先裁剪使用独立 storage buffer，包含逆变换、尺寸和圆角。片元阶段按需检查裁剪链。

普通矩形、圆角矩形和椭圆使用距离场着色器。文字、路径和图像使用缓存纹理。纹理图集按行分配，默认 2048²，多页上限 12；按文档顺序保持 painter's order，连续的兼容纹理批次使用同一次实例化 draw。GPU 网格和图层绘制之后，2D 叠加层绘制选择与辅助信息。

缓存键仅包含会影响光栅外观的属性。移动图层不改变文本或路径纹理；更新文本、尺寸、样式或图像后重新上传相应条目。视口裁剪只提交附近对象；节点达到较大数量时仍会进行 CPU 遍历，并没有空间索引树。

`stats.cpuMs` 为 `performance.now()` 测量的 CPU 准备与提交耗时，并不是 GPU 时间戳；界面明确说明这个区别。

## 4. 文本

文本布局以浏览器 Canvas 的字体度量为基础，按中文字符、拉丁单词与空格换行。常规字距为零时以整行绘制，非零字距或两端对齐时逐字符安排位置。中英文输入在覆盖到画布上的 contenteditable 元素中进行，输入法组合期间不截获全局快捷键。

该实现不是完整的 shaping 引擎。复杂脚本、变体字形、混合字重、基线、首字下沉与 OpenType 特性不在实现范围；不同操作系统字体度量可能产生差异。

## 5. 操作与历史

一次指针手势只记录一个操作前快照。离散属性编辑同样用快照事务包围。历史上限为 70 条，且逐步收缩到约 24 MB 的字符串预算；单条大型快照仍可能超出此预算。

实例保存 masterId，子层保存 sourceId；编辑提交时重新从主组件派生子层，再叠加实例覆盖。仅提供基础关联与覆盖，不实现成熟组件系统的全部能力。

## 6. 存储、输入校验与导出

正常页面来源使用 IndexedDB `moko-design-local/workspace`，当前文件和不同文件 ID 分别记录。IndexedDB 不可用时尝试小容量 localStorage 备份；两者均不可用时明确进入临时编辑，不吞掉保存失败。

导入会检查节点数量、深度、ID、类型、数值、颜色、嵌入图片格式以及路径数据，并拒绝 prototype/constructor 等不安全属性。用户文字与名称渲染到 HTML 时转义。SVG 图片作为图像源处理，不将其内容插入应用 DOM。

PNG 使用共享文档与 Canvas 绘图逻辑生成，并限制最大导出尺寸/面积。SVG 从文档图层生成 path/rect/ellipse/text/image，包含变换、裁剪与基础效果。SVG 不是位图截图，但导入的 SVG 图片仍是一个嵌入 image。

## 7. 后续工程重点

生产化首先应在目标桌面浏览器、真实 GPU 和 HTTPS 环境中完成矩阵验证与性能分析，再处理图集回收、空间索引、增量历史与大文件持久化。随后才适合扩展精确文字 shaping、矢量布尔运算、组件变体与协作数据层。

在没有硬件测试的情况下，本交付不承诺帧率、最大流畅图层数、保存耐久性或 Figma 级兼容性。
